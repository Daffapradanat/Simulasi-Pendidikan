import { Application } from "express";

// Fungsi untuk mengonfigurasi keamanan server (saat ini dinonaktifkan sepenuhnya)
export function configureSecurity(app: Application) {
  // Dinonaktifkan sementara untuk menghindari masalah 502 di lingkungan preview (HTTP)
}
